-- Please see below for the SQL code that I am querying for a database that's holding Nexflix data to answer various questions

-- 1. How many movie titles are there in the database? (excluding tv shows)
SELECT count (*)
FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info"
WHERE type='Movie';

-- 2. When was the most recent batch of tv shows and/or movies added to the database? 
SELECT max(date(date_added))
FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info"

-- 3. List all movies and tv shows, then order them alphabetically. 
SELECT title
FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info"
ORDER BY title asc;



